import React from "react";
import ReactDOM from "react-dom";
import PlayersSelection from "./App";

ReactDOM.render(
  <React.StrictMode>
    <PlayersSelection />
  </React.StrictMode>,
  document.getElementById("root")
);
